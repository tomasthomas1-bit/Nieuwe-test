diff --git a/main.py b/main.py
--- a/main.py
+++ b/main.py
@@ -1,15 +1,22 @@
 # main.py
 from __future__ import annotations
 
+import logging
+import os
+import re
 from datetime import datetime, timedelta, timezone
-import logging
-import os
-import re
 from typing import Any, List, Optional, Tuple
 
 import psycopg2
 from cryptography.fernet import Fernet
 from fastapi import (
     Depends,
     FastAPI,
     HTTPException,
     Request,
     Response,
     WebSocket,
     WebSocketDisconnect,
     status,
 )
 from fastapi.middleware.cors import CORSMiddleware
 from fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm
+from fastapi.templating import Jinja2Templates
+from fastapi.staticfiles import StaticFiles
+from fastapi.responses import HTMLResponse
 from haversine import Unit, haversine
 from jose import JWTError, jwt
 from passlib.context import CryptContext
 from psycopg2.pool import ThreadedConnectionPool
 from pydantic import BaseModel, Field, HttpUrl, validator
 import uvicorn
 
@@ -45,6 +52,9 @@
 # -------------------- App init --------------------
 app = FastAPI(title="Sports Match API", version="2.1.0")
 
+# Templates & static
+templates = Jinja2Templates(directory="templates")
+
 # CORS
 if _allowed_origins:
     app.add_middleware(
@@ -65,6 +75,9 @@
         allow_headers=["*"],
     )
 
+# Serve static assets (CSS, images, JS)
+app.mount("/static", StaticFiles(directory="static"), name="static")
+
 # -------------------- DB Pool & Helpers --------------------
 pool: Optional[ThreadedConnectionPool] = None
 
@@ -180,6 +193,16 @@
     return user
 
 
+# -------------------- Homepage --------------------
+@app.get("/", response_class=HTMLResponse)
+async def home(request: Request):
+    """
+    Een eenvoudige landing page voor de app met links naar docs
+    en live health-status.
+    """
+    return templates.TemplateResponse("index.html", {"request": request})
+
+
 # -------------------- Startup / Shutdown --------------------
 @app.on_event("startup")
 def on_startup():
diff --git a/requirements.txt b/requirements.txt
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,0 +1,10 @@
+# Backend
+fastapi
+uvicorn[standard]
+psycopg2-binary
+passlib[bcrypt]
+python-jose
+cryptography
+haversine
+pydantic
+httpx
+
+# Tooling & tests
+pytest
+ruff==0.6.9
+
+# Templates
+jinja2
diff --git a/templates/index.html b/templates/index.html
new file mode 100644
--- /dev/null
+++ b/templates/index.html
@@ -0,0 +1,148 @@
+<!doctype html>
+<html lang="nl">
+  <head>
+    <meta charset="utf-8">
+    <meta name="viewport" content="width=device-width, initial-scale=1" />
+    <title>Sports Match API</title>
+    https://fonts.googleapis.com
+    <link rel="preconnect" href="https://fonts.gstatic.com" crossoriginmily=Inter:wght@400;600;700&display=swap
+    /static/styles.css
+    <meta name="color-scheme" content="light dark">
+  </head>
+  <body>
+    <header class="container header">
+      <div class="brand">
+        <div class="logo">🏃‍♀️</div>
+        <div class="brand-text">
+          <h1>Sports Match API</h1>
+          <p class="muted">FastAPI backend is live</p>
+        </div>
+      </div>
+      <nav class="nav">
+        /docsSwagger Docs</a>
+        <aedocRedoc</a>
+      </nav>
+    </header>
+
+    <main>
+      <section class="hero">
+        <div class="container grid-2">
+          <div>
+            <h2>Welkom! 👋</h2>
+            <p class="lead">
+              Je backend draait. Gebruik de knoppen om endpoints te verkennen, of check de live status hieronder.
+            </p>
+            <div class="cta">
+              /docsOpen API Docs</a>
+              <a classHealth endpoint</a>
+            </div>
+            <div class="status">
+              <span class="dot" id="status-dot"></span>
+              <span id="status-text">Status ophalen…</span>
+            </div>
+          </div>
+          <div class="card">
+            <h3>Handige links</h3>
+            <ul class="links">
+              <li>/users/1Voorbeeld: gebruikersprofiel <code>/users/&lt;id&gt;</code></a></li>
+              <li>/matchesMatches <code>/matches</code></a></li>
+              <li><auggestionsSuggesties <code>/suggestions</code> (auth vereist)</a></li>
+            </ul>
+            <p class="muted small">
+              Sommige endpoints vereisen authenticatie. Haal een token op via <code>/token</code> of gebruik Swagger.
+            </p>
+          </div>
+        </div>
+      </section>
+
+      <section class="container">
+        <div class="grid-3">
+          <div class="feature">
+            <div class="feature-icon">⚡</div>
+            <h4>Snel</h4>
+            <p>FastAPI met async I/O en uvicorn voor hoge performance.</p>
+          </div>
+          <div class="feature">
+            <div class="feature-icon">🔒</div>
+            <h4>JWT‑auth</h4>
+            <p>Veilige toegang met bearer tokens, cookies en scopes.</p>
+          </div>
+          <div class="feature">
+            <div class="feature-icon">🗺️</div>
+            <h4>Geo &amp; SQL</h4>
+            <p>Haversine‑berekeningen en Postgres met efficiënte queries.</p>
+          </div>
+        </div>
+      </section>
+    </main>
+
+    <footer class="footer">
+      <div class="container footer-inner">
+        <span>© <span id="year"></span> Sports Match</span>
+        <div class="footer-links">
+          /docsDocs</a>
+          /redocRedoc</a>
+          https://web-production-ec0bc.up.railway.app/healthzStatus</a>
+        </div>
+      </div>
+    </footer>
+
+    <script>
+      // jaar in footer
+      document.getElementById('year').textContent = new Date().getFullYear();
+      // live status ophalen
+      async function fetchStatus() {
+        const dot = document.getElementById('status-dot');
+        const text = document.getElementById('status-text');
+        try {
+          const res = await fetch('/healthz', {cache: 'no-store'});
+          if (!res.ok) throw new Error('HTTP ' + res.status);
+          const data = await res.json();
+          if (data && data.status === 'ok') {
+            dot.classList.remove('red', 'amber');
+            dot.classList.add('green');
+            text.textContent = 'Gezond (200 OK)';
+          } else {
+            dot.classList.remove('green', 'red');
+            dot.classList.add('amber');
+            text.textContent = 'Onzeker (respons onverwacht)';
+          }
+        } catch (e) {
+          dot.classList.remove('green', 'amber');
+          dot.classList.add('red');
+          text.textContent = 'Niet bereikbaar';
+        }
+      }
+      fetchStatus();
+      setInterval(fetchStatus, 10000);
+    </script>
+  </body>
+  </html>
diff --git a/static/styles.css b/static/styles.css
new file mode 100644
--- /dev/null
+++ b/static/styles.css
@@ -0,0 +1,243 @@
+:root{
+  --bg: #0b0c10;
+  --panel: #111318;
+  --muted: #a6adbb;
+  --text: #e8eaed;
+  --brand: #5b9cff;
+  --brand-2:#7bd389;
+  --border:#23262d;
+  --shadow: 0 10px 30px rgba(0,0,0,.35);
+}
+@media (prefers-color-scheme: light){
+  :root{
+    --bg:#f6f7fb;
+    --panel:#ffffff;
+    --muted:#5b6473;
+    --text:#0f1222;
+    --brand:#2b6ef6;
+    --brand-2:#1fb370;
+    --border:#e9ecf2;
+    --shadow: 0 10px 30px rgba(16,24,40,.08);
+  }
+}
+*{box-sizing:border-box}
+html,body{height:100%}
+body{
+  margin:0;
+  font-family: Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji","Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
+  color:var(--text);
+  background:linear-gradient(180deg, var(--bg), #0d0f15 40%, var(--bg));
+}
+.container{width:min(1100px, 92%); margin-inline:auto}
+.muted{color:var(--muted)}
+.small{font-size:.9rem}
+.header{
+  display:flex; align-items:center; justify-content:space-between;
+  padding:22px 0;
+}
+.brand{display:flex; align-items:center; gap:14px}
+.logo{
+  width:44px;height:44px; display:grid; place-items:center;
+  border-radius:12px; background:var(--panel); box-shadow:var(--shadow); border:1px solid var(--border);
+  font-size:20px;
+}
+.brand-text h1{font-size:20px; margin:0 0 2px}
+.nav{display:flex; gap:10px}
+.btn{
+  display:inline-flex; align-items:center; gap:8px;
+  border-radius:10px; padding:10px 14px; text-decoration:none; font-weight:600;
+  border:1px solid transparent; transition:.2s background-color, .2s border-color, .2s color;
+}
+.btn.primary{background:var(--brand); color:white; box-shadow:var(--shadow)}
+.btn.secondary{background:var(--panel); border-color:var(--border); color:var(--text)}
+.btn.ghost{background:transparent; border-color:var(--border); color:var(--text)}
+.btn:hover{filter:brightness(1.05)}
+
+.hero{
+  padding:40px 0 20px;
+}
+.grid-2{
+  display:grid; grid-template-columns: 1.2fr .8fr; gap:28px; align-items:stretch;
+}
+.grid-3{
+  display:grid; grid-template-columns: repeat(3, 1fr); gap:22px; margin: 24px 0 60px;
+}
+.lead{font-size:1.05rem; color:var(--muted)}
+.cta{display:flex; gap:12px; margin:18px 0 8px}
+.status{display:flex; align-items:center; gap:10px; margin-top:8px; color:var(--muted)}
+.dot{
+  width:10px; height:10px; border-radius:50%; background:#777; display:inline-block;
+  box-shadow: 0 0 0 3px rgba(123,211,137,.12);
+}
+.dot.green{background:var(--brand-2)}
+.dot.red{background:#ff6b6b; box-shadow: 0 0 0 3px rgba(255,107,107,.12)}
+.dot.amber{background:#f7b500; box-shadow: 0 0 0 3px rgba(247,181,0,.12)}
+
+.card{
+  background:var(--panel); border:1px solid var(--border); border-radius:16px; padding:20px;
+  box-shadow:var(--shadow); height:100%;
+}
+.card h3{margin:0 0 10px}
+.links{list-style:none; padding-left:0; margin:8px 0 0; display:grid; gap:10px}
+.links a{color:var(--text); text-decoration:none; border-bottom:1px dashed transparent}
+.links a:hover{border-color:var(--border)}
+.feature{
+  background:var(--panel); border:1px solid var(--border); border-radius:14px; padding:18px;
+  box-shadow:var(--shadow)
+}
+.feature-icon{font-size:22px; margin-bottom:10px}
+
+.footer{
+  border-top:1px solid var(--border);
+  background: linear-gradient(180deg, transparent, rgba(0,0,0,.08));
+}
+.footer-inner{
+  display:flex; align-items:center; justify-content:space-between; padding:22px 0; color:var(--muted)
+}
+.footer-links{display:flex; gap:14px}
+.footer a{color:var(--muted); text-decoration:none}
+.footer a:hover{color:var(--text)}
+
+@media (max-width: 880px){
+  .grid-2{grid-template-columns: 1fr}
+  .grid-3{grid-template-columns: 1fr}
+  .header{flex-direction:column; gap:12px}
+  .nav{flex-wrap:wrap; justify-content:center}
